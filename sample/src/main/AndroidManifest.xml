<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.me.ui.sample"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 适配全面屏 -->
        <meta-data
            android:name="android.max_aspect"
            android:value="2.4"/>

        <activity
            android:name="com.me.ui.sample.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ARouter -->
        <activity
            android:name=".thirdparty.arouter.activity.RouterTestActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".thirdparty.arouter.activity.RouterSchemeActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".thirdparty.arouter.activity.SchemeFilterActivity">
            <intent-filter>
                <data
                    android:host="kylingo.com"
                    android:scheme="arouter"/>

                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".widget.basic.StatusBarColorActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".widget.basic.StatusBarImageActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity
            android:name=".widget.basic.InputMethodActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".library.basic.DataBindingActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>